class OnboardingData {
  final String title;
  final String description;
  // In a real app, you'd likely have an image asset path here too.
  // final String imagePath;

  OnboardingData({
    required this.title,
    required this.description,
    // required this.imagePath,
  });
}
